---
import BlogPosts from "../components/BlogPosts.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import "../styles/global.css";
const allPosts = await Astro.glob("./posts/*.md");
---

<BaseLayout>
  <h1>The Boa language</h1>

  <p>This will be where I write about my programming language</p>
  <ul>
    {
      allPosts.map((post) => (
        <BlogPosts title={post.frontmatter.title} url={post.url} />
      ))
    }
  </ul>
</BaseLayout>
